// "use strict";
//doesn't work like this in react, use strictmode react
//helps prevent errors, compiler errors as well
//shows the error versus it staying a silent error



const Header = () => {
    return <div className="App-header">I❤️React</div>
}

export default Header